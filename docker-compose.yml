services:
  frontend:
    build:
      context: .
      args:
        # URL appelee par le NAVIGATEUR -> doit etre joignable cote client.
        # En prod: https://api.ton-domaine
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
    restart: unless-stopped
    ports:
      - "3000:3000"
