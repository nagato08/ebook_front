services:
  frontend:
    build:
      context: .
      args:
        # URL appelee par le NAVIGATEUR -> doit etre joignable cote client.
        # En prod: https://api.ton-domaine
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:3001}
        # URL publique du site (SEO). En prod: https://ton-domaine
        NEXT_PUBLIC_SITE_URL: ${NEXT_PUBLIC_SITE_URL:-http://localhost:3000}
    restart: unless-stopped
    ports:
      - "3000:3000"
